import React from "react";
import { withRouter } from "react-router-dom";
import "./Contact.css";

function Contact() {
  return (
    <body>
      CONTACT
    </body>
  );
}

export default withRouter(Contact);
