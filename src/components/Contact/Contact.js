import React from "react";
import { withRouter } from "react-router-dom";
import "./Contact.css";

function Contact() {
  return (
    <div>
      CONTACT
    </div>
  );
}

export default withRouter(Contact);
